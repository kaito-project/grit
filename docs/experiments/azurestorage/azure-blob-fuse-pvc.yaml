apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: azure-blob-fuse-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: azureblob-fuse-premium
  resources:
    requests:
      storage: 256Gi